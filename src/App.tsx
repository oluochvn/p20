import { useState } from "react";

function App(){
          const [dark,setDark] = useState(false)
          const [open, setOpen] = useState(false);

  return(

    <div>
        hh
    </div>
  )
}
export default App;